{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        // "build": "tsc -b --clean packages/server && cd packages/client && npm build",
        "dev:server": "nodemon --exec run-s -s build:server start:server",
        "build:server": "tsc -b -c packages/server",
        "start:server": "node server/build/app.js"
        // "start": "nodemon packages/server & (cd packages/client && npm start)"
    },
    "devDependencies": {
        "npm-run-all": "^4.1.5",
        "tslint": "^5.15.0",
        "typescript": "^3.4.3"
    }
}
