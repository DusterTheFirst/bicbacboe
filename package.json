{
    "private": true,
    "workspaces": [
        "client",
        "server",
        "protocol"
    ],
    "scripts": {
        "dev:server": "nodemon --config server/nodemon.json",
        "dev:client": "cd client && yarn start",
        "clean:server": "tsc -b --clean server",
        "clean:protocol": "tsc -b --clean protocol",
        "clean": "run-p clean:**",
        "force-clean": "rimraf {client,server,protocol}/build",
        "build:server": "tsc -b server",
        "start:server": "node server",
        "lint:server": "tslint -p server/tsconfig.json",
        "lint:protocol": "tslint -p protocol/tsconfig.json",
        "lint:client": "tslint -p client/tsconfig.json",
        "lint": "run-p -c lint:**"
    },
    "devDependencies": {
        "npm-run-all": "^4.1.5",
        "rimraf": "^2.6.3",
        "tslint": "^5.16.0",
        "typescript": "^3.4.3"
    }
}
