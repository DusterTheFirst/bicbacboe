language: node_js
node_js:
  - "11"
cache: yarn
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.15.2
  - export PATH="$HOME/.yarn/bin:$PATH"
jobs:
  include:
    - stage: build
      script: yarn build
    # TODO: Test
    # TODO: allow fail
    - stage: lint
      script: yarn lint
    # TODO: Deploy